# ldDecrypt 绿盾解密工具 · 开发过程归档

> 归档时间：2026-08-03
> 目的：完整记录本项目的开发迭代、诊断过程、关键决策与踩坑，便于后续回溯与接手。
> 配套文档：`使用说明.md`（面向使用者）、`readme.md`（原项目说明）、本文件（面向开发者/回溯）。

---

## 0. 一句话结论

本工具**不破解**天锐绿盾，而是利用绿盾客户端的「读时自动解密」过滤驱动，用字节拷贝把明文取出来。整盘受绿盾监控的机器上，**任何本地落盘都会被重新加密**，唯一可靠出路是把明文字节流经网络推到另一台不受管控的机器（网络导出）。全部代码已完整可用，最终形态为：Web 工具 + 零依赖接收端 + 可对话框调用的 Skill。

---

## 1. 项目背景与目标

- **对象**：天锐绿盾（Green Shield）透明加密文件。
- **核心约束**：文件在磁盘上是密文，只有在装有绿盾客户端且已授权的机器上、被进程读取时，过滤驱动才透明返回明文。
- **目标**：在不破解算法、不接触密钥的前提下，把明文提取出来供正常使用。
- **关键事实（已用户验证）**：本机是 A 类——`fs.read` 能读到明文（诊断 `head=%PDF-1.5` / `PK` 等魔数均命中），证明读钩子生效；但本机**整盘受绿盾监控**，本地任何写入都被写时加密。

---

## 2. 开发环境与关键约束（踩坑记录）

| 约束 | 现象 | 应对 |
|------|------|------|
| 沙箱写入中文按 GBK | 编辑后的 `.ejs/.js/.css` 打开是乱码、语法偶发异常 | 每次改动后用 Python 统一转码为 UTF-8 再校验 |
| 沙箱自身加密层 | 任意写入文件读回头部为 `89 7d 1c`（假密文），干扰 A/B 类判断 | 以用户本机诊断为准，沙箱仅验证代码逻辑 |
| `bash rm -rf` 被拦截 | 删除失败（沙箱安全删除机制） | 改用 `PowerShell Remove-Item -Recurse -Force` |
| 运行时路径 | 使用托管运行时 | node：`C:/Users/Administrator/.workbuddy/binaries/node/versions/22.22.2/node.exe`；python：`C:/Users/Administrator/.workbuddy/binaries/python/versions/3.13.12/python.exe` |
| 默认服务端口 | Web 主服务 | `3000`；接收端默认 `4000` |

---

## 3. 核心原理（三铁律）

1. **读时解密**：进程读取加密文件时，绿盾过滤驱动透明返回明文。因此「字节拷贝」（读取即明文）即可提取。
2. **写时加密**：任何进程把文件写入**受绿盾管控的目录/盘符**，会被重新加密。所以「输出到哪」比「怎么读」更关键。
3. **网络出口不拦**：绿盾只拦本机磁盘写入，不拦网络。把明文字节 POST 到另一台不受管控的机器落盘，可彻底绕过写时加密。

### A 类 vs B 类诊断

| 类型 | 文件头表现 | 含义 | 处置 |
|------|-----------|------|------|
| A 类 | 命中明文魔数（`%PDF`、`PK`/ZIP、`PNG`、`JPG`、`OLE`/XLS 等） | 读钩子生效，读出即明文 | 可直接字节拷贝 / 网络导出 |
| B 类 | 异常头（沙箱常见 `89 7D 1C` 或绿盾密文头） | 读钩子未给明文（未授权/未装客户端/需白名单） | 需绿盾控制台把 `node.exe` 或目录加白名单 |

> ⚠️ **读回无法自检**：受控机上读回「被重新加密的文件」也会因读钩子显示明文，故不能靠 `head` 判断本地落盘是否被重新加密，必须用**无绿盾机器**打开确认。

---

## 4. 完整开发迭代时间线

1. **分析全目录**：发现 2 个 `.ejs` 为损坏二进制（被绿盾加密），`public/` 缺失，无鉴权。
2. **重建 UI**：重建 `index.ejs`/`monitor.ejs`，新建 `public/style.css`；沙箱 GBK→UTF-8 转码。
3. **批量解密修复**：新增 `lib/zip.js`（零依赖 ZIP）、`POST /api/decrypt/batch` 与 UI；修复 `fs.unlinkSync` 在循环内崩溃的 bug（改 `safeUnlink`/`safeRm`）。
4. **ZIP 文件名乱码**：根因为 busboy 的 Latin-1 误判导致双编码；新增 `decodeUploadName()`（Latin-1→UTF-8，带码点 ≤0xFF 守卫），应用于单文件与批量路由。
5. **解释目录监控**：② 为事件驱动 watcher（chokidar），确认可工作；用户本机确有 `D:/fileWatch`。
6. **配置持久化**：新增 `config.json`（`saveConfig/loadConfig`）、`POST /api/scan`、`GET /api/watch` 状态面板（徽章 + 统计）。
7. **输出未解密**：发现写时加密；默认输出改 `D:/ldDecrypt/decrypted/watch_out`，更新 `config.json`。
8. **仍密文**：在 `lib/decrypt.js` 加「临时目录 + rename」兜底（`useRename:true`），写入 4 个调用点；请用户诊断 → `head=255044462d312e35`（`%PDF-1.5`）确认 A 类。
9. **执行双方案**：输出改安全区 `D:/ldDecrypt/decrypted` + `KNOWN_SAFE_DIR` 守卫 + ⚠️ 警告；更新 `config.json`/`index.ejs`/`style.css`。
10. **整盘受控，放弃本地**：用户确认「这个目录也是被监控的」→ 整盘受控下本地落盘必然失败，本地路线放弃。
11. **方案4 网络导出落地**：新增 `lib/exportClient.js`（流式 `exportStream` + 缓冲 `exportBuffer`）、`export-receiver.js`（零依赖接收端）；①/③/②/扫描 在 `exportUrl` 非空时改走网络导出（读流直推，不写本地）；回环测试通过（含中文名/子目录、令牌 401 拒绝）。
12. **接收端一键启动**：`run-receiver.bat`（双击即运行，优先调 `ldDecrypt-receiver.exe` 否则用 Node）；`export-receiver.js` 增强 `--port/-p`、`--dir/-d`、`--token/-t`、默认落盘到自身目录、端口冲突/崩溃友好提示。尝试 `pkg` 打单文件 exe 因沙箱封锁 GitHub（`ECONNRESET`）未成，已在使用说明第 10.3 节给出用户侧自行打包命令。
13. **沉淀为 Skill**：创建用户级 `greenshield-decrypt`（`~/.workbuddy/skills/`），含 `SKILL.md`、`scripts/diag.js`、`scripts/decrypt.js`、`references/diagnosis.md`、`references/ldDecrypt-tool.md`。
14. **Skill 增强 `--mode`**：`decrypt.js` 支持 `--mode local|network`；网络模式自动读 `config.json` 的 `exportUrl/exportToken`，支持 `--url/--token/--tool` 覆盖、目录递归。
15. **实战解密**：`房角镜.xlsx` 经网络导出成功推到 `192.168.0.119`；`digital_26127000000226702959.pdf` 经本地模式落到 `D:/ldDecrypt/decrypted/`，因整盘受控需用户自行在无绿盾机器验证真伪。

---

## 5. 网络导出方案详解

### 5.1 为什么本地不行
绿盾按「写入位置」加密：文件落在被保护目录/盘符，任何进程写进去都被过滤器重新加密。`rename` 兜底失效是因为临时文件本身就在管控树内，一写进临时区已是密文。只要输出在监控范围，无论怎么写、怎么改名，落盘都是密文。

### 5.2 架构
```
[受控机·装绿盾·整盘受控]
   node 读取加密文件 → 读钩子返回明文 → 流式读出明文
        │  (HTTP POST，仅网络出口，绿盾不拦)
        ▼
[接收端机·不受绿盾保护]
   export-receiver.js 监听 :4000
        → 按 X-Relpath 落盘到 ./received/
```
受控机全程不碰本机磁盘明文，明文只在网络传输与接收端落盘时存在，故保得住。

### 5.3 协议
- 接收端：`POST /upload`，请求体为原始字节。
- 请求头：`X-Filename`（文件名）、`X-Relpath`（相对路径，用于保留子目录）、`Authorization`（令牌，格式 `Bearer <token>`，空则放行）。
- 校验：防目录穿越（`path.normalize` + 前缀校验）；令牌不匹配返回 401。

### 5.4 部署步骤
1. **接收端机**（不受绿盾保护）：进入 `ldDecrypt`，双击 `run-receiver.bat`（或 `npm run receiver` / `node export-receiver.js`）；记下内网 IP 与 `:4000/upload`。
2. **受控机**：`npm start`，浏览器开 `http://localhost:3000`；④ 网络导出填 `http://<接收端IP>:4000` + 令牌 → 保存并启用。
3. 之后 ①/③/②/扫描 全部自动把明文推到接收端。

---

## 6. 接收端一键启动

### 6.1 `run-receiver.bat`
与 `export-receiver.js` 同目录，双击即运行。启动逻辑：
- 同目录若存在 `ldDecrypt-receiver.exe`（真正单文件 exe），优先用它（连 Node 都不用装）；
- 否则 `where node` 存在则用 `node export-receiver.js`；
- 都没有则提示装 Node 并 `pause` 不闪退。
- 顶部带 `rem set TOKEN= / set PORT= / set DIR=` 配置项。

### 6.2 `export-receiver.js` 参数
| 参数 | 说明 | 默认 |
|------|------|------|
| `--port` / `-p` | 监听端口 | 4000（或 `PORT` 环境变量） |
| `--dir` / `-d` | 保存目录 | 程序自身目录 `\received`（或 `DIR`） |
| `--token` / `-t` | 令牌（建议设置） | 空（或 `TOKEN`） |

> 单文件 exe 备选（在能访问 GitHub 的机器）：`npm install -g pkg` → `pkg export-receiver.js --targets node18-win-x64 --output ldDecrypt-receiver.exe`。生成的 exe 拷过去双击即跑，`bat` 会自动优先调用。

---

## 7. Skill 化（greenshield-decrypt）

位置（用户级，跨项目可用）：`C:/Users/Administrator/.workbuddy/skills/greenshield-decrypt/`

| 文件 | 作用 |
|------|------|
| `SKILL.md` | 触发条件、前置条件、三铁律、A/B 类诊断、三种工作流（本地/网络/工具）、关键陷阱 |
| `scripts/diag.js` | 打印文件头 16 字节并判别 A/B 类 |
| `scripts/decrypt.js` | 零依赖解密：`--mode local`（字节拷贝）/`--mode network`（推接收端）；支持目录递归、`--flat`、`--url/--token/--tool` |
| `references/diagnosis.md` | 原理与排查表 |
| `references/ldDecrypt-tool.md` | `D:/ldDecrypt` 工具架构 |

**用法示例**
```bash
node diag.js "D:/fileWatch/a.pdf"                       # 诊断 A/B 类
node decrypt.js "D:/fileWatch/a.pdf" "D:/safe/out" --mode local
node decrypt.js "D:/fileWatch/a.pdf" --mode network    # 推到 config.json 的接收端
node decrypt.js "D:/fileWatch" --mode network           # 整目录递归推送
```

---

## 8. 实战解密记录

### 8.1 房角镜.xlsx（网络导出）
- diag：`50 4b 03 04` → A 类（XLSX/ZIP 明文）。
- 因整盘受控，走网络导出：临时脚本读源（读钩子给明文）并流式 POST 到 `config.json` 接收端 `http://192.168.0.119:4000/upload`（token `abc123`）→ HTTP 200，明文落接收端 `received/`。本机零落盘。
- 临时脚本已删，原始文件未移动/修改。

### 8.2 digital_26127000000226702959.pdf（本地模式）
- diag：`255044462d312e37` → A 类（%PDF-1.7，98168 字节）。
- 用户要求「解密到本地」→ `decrypt.js --mode local` 落到 `D:/ldDecrypt/decrypted/`，exit=0，大小一致。
- ⚠️ 因整盘受控，本地落盘可能被写时加密；且本机读回会显示明文无法自检真伪。用户选择「保留本地文件自验」，拟拷至无绿盾机器（接收端 `192.168.0.119`）打开确认；若密文则改走网络导出。

---

## 9. 文件改动总清单

| 文件 | 改动 | 状态 |
|------|------|------|
| `index.js` | 主服务；decodeUploadName、批量路由、config 持久化、扫描、④ 导出配置接口、四条路径接网络导出 | 多次迭代 |
| `views/index.ejs` | ①/②/③/④ UI、下拉选择本地/网络、状态面板 | 多次迭代 |
| `views/monitor.ejs` | 日志轮询页 | 重建 |
| `public/style.css` | 样式、安全区提示、徽章 | 创建+改 |
| `lib/decrypt.js` | 字节拷贝 + 临时目录 rename 兜底（`useRename`） | 重写 |
| `lib/zip.js` | 零依赖 ZIP（store + CRC32 + UTF-8 文件名 `0x0800`） | 新建 |
| `lib/exportClient.js` | 网络导出客户端（流式/缓冲） | 新建 |
| `export-receiver.js` | 零依赖接收端；支持 `--port/--dir/--token`、友好错误处理 | 新建+增强 |
| `run-receiver.bat` | 双击一键启动接收端 | 新建 |
| `config.json` | 持久化 `sourceDir`/`targetDir`/`exportUrl`/`exportToken` | 编辑 |
| `package.json` | 增加 `receiver` 脚本 | 编辑 |
| `readme.md` | 补充「方式四 网络导出」 | 编辑 |
| `使用说明.md` | 面向使用者的完整说明 | 新建 |
| `开发归档.md` | 本文档 | 新建 |
| `~/.workbuddy/skills/greenshield-decrypt/**` | 对话框可调用 Skill | 新建+增强 |

---

## 10. 故障排查表

| 现象 | 根因 | 处置 |
|------|------|------|
| 网页打不开 | 开了 `:4000`（接收端无页面） | 主服务在 `:3000`，`npm start` 后开 `http://localhost:3000` |
| `npm : 无法识别` | 接收端机器未装 Node / 未加 PATH | 装 Node 或 `node export-receiver.js`（零依赖） |
| 连接拒绝/超时 | 防火墙未放行、IP 不通、填了 localhost | 放行端口；受控机填接收端**内网 IP**；`Test-NetConnection` 测通 |
| 解密后仍是密文 | 写时加密（输出目录在管控内） | 换安全目录或走网络导出 |
| 本地 `head=%PDF` 但疑似密文 | 读回陷阱：读钩子解密一切 | 必须在**无绿盾机器**打开验证 |
| 批量 ZIP 文件名乱码 | busboy Latin-1 双编码 | `decodeUploadName()` 守卫已修复 |
| 接收端 401 | 令牌不匹配 | 受控机④与接收端 `--token`/`TOKEN` 保持一致 |
| 接收端 EADDRINUSE | 端口被占 | 换 `--port` 或 `PORT` |

---

## 11. 后续可优化点

- `decrypt.js` 网络模式增加「失败自动回退本地」与「推送后回读校验」（需接收端配合回传哈希）。
- 接收端增加多令牌、HTTPS、上传限流。
- 把 Skill 从用户级移到 `D:/ldDecrypt/.workbuddy/skills/`（项目级）随仓库共享。
- 主服务 ④ 启用时给 ①/③ 的默认下拉自动跟随；批量失败时列出具体失败文件名。
- 若绿盾后续 hook `rename`，需依赖控制台白名单，届时更新诊断文档。
